import React, { forwardRef, memo } from 'react'

const BabelForm = forwardRef(
    (
        {
            uiSchema: initUiSchema = {},
            dataSchema: initDataSchema = {},
            formData: initFormData = {},
            onQuery = {},
        },
        ref
    ) => {

        return <div>

        </div>
    }
)
